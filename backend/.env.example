# Скопируйте этот файл в .env на сервере и заполните значения.
# Файл .env не должен попадать в репозиторий.

NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-on-server

# Домен фронтенда для CORS (без слеша в конце)
CORS_ORIGIN=https://eioven-mesto.nomorepartiessite.ru
